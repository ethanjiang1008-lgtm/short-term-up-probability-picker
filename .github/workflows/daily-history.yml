name: Daily Candidate History

on:
  workflow_dispatch:
  schedule:
    - cron: "25 7 * * 1-5"

permissions:
  contents: write

jobs:
  snapshot:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Install dependencies
        run: pip install -r requirements.txt
      - name: Run tests
        run: pytest scripts/tests -q
      - name: Build daily snapshot
        run: python scripts/daily_pipeline.py
      - name: Label previous snapshots
        run: python scripts/label_history.py
      - name: Build threshold report
        run: python scripts/report.py
      - name: Commit data
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -f data/
          git diff --cached --quiet || git commit -m "data: daily candidate history"
          git push
